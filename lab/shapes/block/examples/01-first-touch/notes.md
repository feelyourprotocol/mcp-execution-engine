# First-touch value block (Amsterdam)

One 1 wei transfer from `0x…ee` to empty `0x…aa`, executed as a **block**. Paid tx gas is about **204600**. `header.gasUsed` is the generated header field (on Amsterdam it may track the state-gas dimension). Repeat with `"baseHardfork": "osaka"` for a 21000 paid tx.
