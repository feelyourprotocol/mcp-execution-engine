# Generate (planned)

> **Status:** Not implemented in the engine yet. **Step 6** — EIP-7928 block-level access lists.

## What it will do

Produce structured protocol outputs such as **block-level access lists (BAL)** via `createVM` + `runBlock` + `blockLevelAccessList` — same patterns as the website [EIP-7928 exploration](https://feelyourprotocol.org).

## Future MCP tool

`generate_eip7928_bal` on the gateway.

## Until then

- Interactive reference: website EIP-7928 exploration
- MCP docs: [Coverage](https://mcp-docs.feelyourprotocol.org/use/coverage.html) (7928 listed as `generate, simulate`)

Examples and schemas will be added here when Step 6 lands.
